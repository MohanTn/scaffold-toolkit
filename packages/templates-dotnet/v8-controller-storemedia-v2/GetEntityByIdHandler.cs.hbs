namespace {{ns}}.V2.Application.Handlers;

public class Get{{entity}}ByIdHandler
    : {{ns}}.MediatR.IQueryHandler<{{ns}}.V2.Application.Queries.Get{{entity}}ByIdQuery, {{ns}}.V2.Application.DTOs.{{entity}}Dto?>
{
    private readonly {{ns}}.V2.Infrastructure.Repositories.I{{entity}}Repository _repository;

    public Get{{entity}}ByIdHandler({{ns}}.V2.Infrastructure.Repositories.I{{entity}}Repository repository)
    {
        _repository = repository;
    }

    public async Task<{{ns}}.V2.Application.DTOs.{{entity}}Dto?> HandleAsync({{ns}}.V2.Application.Queries.Get{{entity}}ByIdQuery query, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        var entity = await _repository.GetByIdAsync(query.Id, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return entity is null ? null : new {{ns}}.V2.Application.DTOs.{{entity}}Dto(entity.Id{{#each fields}}{{#unless (eq (pascal name) "Id")}}, entity.{{pascal name}}{{/unless}}{{/each}});
    }
}
