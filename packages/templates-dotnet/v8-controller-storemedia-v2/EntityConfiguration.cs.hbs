namespace {{ns}}.V2.Infrastructure.Configurations;

public class {{entity}}Configuration : Microsoft.EntityFrameworkCore.IEntityTypeConfiguration<{{ns}}.V2.Domain.Entities.{{entity}}>
{
    public void Configure(Microsoft.EntityFrameworkCore.Metadata.Builders.EntityTypeBuilder<{{ns}}.V2.Domain.Entities.{{entity}}> builder)
    {
        builder.HasKey(x => x.Id);
        // SCAFFOLD:AI_IMPLEMENTATION:START
        // Configure column types, lengths, indexes and relationships for {{entity}}.
        // SCAFFOLD:AI_IMPLEMENTATION:END
    }
}
