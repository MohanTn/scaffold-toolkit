using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc;
using {{ns}}.Application.DTOs;
using {{ns}}.Application.Interfaces;

namespace {{ns}}.Presentation.Controllers;

[ApiController]
[Route("{{route}}")]
public class {{entity}}Controller : ControllerBase
{
    private readonly I{{entity}}Service _service;

    public {{entity}}Controller(I{{entity}}Service service)
    {
        _service = service;
    }

    [HttpGet("{id:guid}")]
    public async Task<ActionResult<{{entity}}Dto?>> GetByIdAsync(Guid id, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        var result = await _service.GetByIdAsync(id, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return result is null ? NotFound() : Ok(result);
    }

    [HttpGet]
    public async Task<ActionResult<IReadOnlyList<{{entity}}Dto>>> ListAsync(CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        var results = await _service.ListAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return Ok(results);
    }

    [HttpPost]
    public async Task<ActionResult<{{entity}}Dto>> CreateAsync({{entity}}Dto dto, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        var created = await _service.CreateAsync(dto, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return CreatedAtAction(nameof(GetByIdAsync), new { id = created.Id }, created);
    }

    [HttpPut("{id:guid}")]
    public async Task<IActionResult> UpdateAsync(Guid id, [FromBody] {{entity}}Dto dto, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        await _service.UpdateAsync(id, dto, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return NoContent();
    }

    [HttpDelete("{id:guid}")]
    public async Task<IActionResult> DeleteAsync(Guid id, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        await _service.DeleteAsync(id, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return NoContent();
    }
}
