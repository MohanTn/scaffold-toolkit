namespace {{ns}}.Application.UnitTests.Features.{{plural entity}};

public class Get{{entity}}ListQueryHandlerTests
{
    private readonly Mock<{{ns}}.Application.Common.Interfaces.I{{entity}}Repository> _repository = new();

    [Fact]
    public async Task Handle_ShouldReturnAllEntities()
    {
        // Arrange
        _repository.Setup(r => r.GetAllAsync(It.IsAny<CancellationToken>()))
            .ReturnsAsync(new List<{{ns}}.Domain.Entities.{{entity}}> { new() { Id = Guid.NewGuid() } });
        var handler = new {{ns}}.Application.Features.{{plural entity}}.Queries.Get{{entity}}List.Get{{entity}}ListQueryHandler(_repository.Object);

        // Act
        var result = await handler.Handle(new {{ns}}.Application.Features.{{plural entity}}.Queries.Get{{entity}}List.Get{{entity}}ListQuery(), CancellationToken.None);

        // Assert
        // SCAFFOLD:AI_IMPLEMENTATION:START
        result.Should().HaveCount(1);
        // SCAFFOLD:AI_IMPLEMENTATION:END
    }
}
