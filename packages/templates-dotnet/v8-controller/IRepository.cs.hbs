using {{ns}}.Domain.Entities;

namespace {{ns}}.Application.Interfaces;

public interface I{{entity}}Repository : IRepository<{{entity}}>
{
    // Add {{entity}}-specific queries here (eg. GetByEmailAsync, FindPagedAsync).
}
