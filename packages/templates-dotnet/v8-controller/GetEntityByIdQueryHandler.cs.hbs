namespace {{ns}}.Application.Features.{{plural entity}}.Queries.Get{{entity}}ById;

public class Get{{entity}}ByIdQueryHandler : IRequestHandler<Get{{entity}}ByIdQuery, {{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto?>
{
    private readonly {{ns}}.Application.Common.Interfaces.I{{entity}}Repository _repository;

    public Get{{entity}}ByIdQueryHandler({{ns}}.Application.Common.Interfaces.I{{entity}}Repository repository)
    {
        _repository = repository;
    }

    public async Task<{{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto?> Handle(Get{{entity}}ByIdQuery request, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START
        var entity = await _repository.GetByIdAsync(request.Id, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return entity is null ? null : new {{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto({{#each fields}}entity.{{pascal name}}{{#unless @last}}, {{/unless}}{{/each}});
    }
}
