namespace {{ns}}.Application.Features.{{plural entity}}.Commands.Delete{{entity}};

public class Delete{{entity}}CommandHandler : IRequestHandler<Delete{{entity}}Command, Unit>
{
    private readonly {{ns}}.Application.Common.Interfaces.I{{entity}}Repository _repository;
    private readonly {{ns}}.Application.Common.Interfaces.IUnitOfWork _unitOfWork;

    public Delete{{entity}}CommandHandler({{ns}}.Application.Common.Interfaces.I{{entity}}Repository repository, {{ns}}.Application.Common.Interfaces.IUnitOfWork unitOfWork)
    {
        _repository = repository;
        _unitOfWork = unitOfWork;
    }

    public async Task<Unit> Handle(Delete{{entity}}Command request, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entity = await _repository.GetByIdAsync(request.Id, cancellationToken);
        if (entity is null) throw new {{ns}}.Application.Common.Exceptions.NotFoundException(nameof({{ns}}.Domain.Entities.{{entity}}), request.Id);
        await _repository.RemoveAsync(entity, cancellationToken);
        await _unitOfWork.SaveChangesAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return Unit.Value;
    }
}
