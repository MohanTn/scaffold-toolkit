using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using {{ns}}.Application.Common;
using {{ns}}.Application.DTOs;
using {{ns}}.Application.Exceptions;
using {{ns}}.Application.Interfaces;
using {{ns}}.Domain.Entities;

namespace {{ns}}.Application.Services;

public class {{entity}}Service : I{{entity}}Service
{
    private readonly I{{entity}}Repository _{{camel entity}}Repository;
    private readonly IUnitOfWork _unitOfWork;

    public {{entity}}Service(I{{entity}}Repository {{camel entity}}Repository, IUnitOfWork unitOfWork)
    {
        _{{camel entity}}Repository = {{camel entity}}Repository;
        _unitOfWork = unitOfWork;
    }

    public async Task<{{entity}}Dto?> GetByIdAsync(Guid id, CancellationToken cancellationToken = default)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entity = await _{{camel entity}}Repository.GetByIdAsync(id, cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return entity is null ? null : MapToDto(entity);
    }

    public async Task<IReadOnlyList<{{entity}}Dto>> ListAsync(CancellationToken cancellationToken = default)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entities = await _{{camel entity}}Repository.ListAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return entities.Select(MapToDto).ToList();
    }

    public async Task<{{entity}}Dto> CreateAsync({{entity}}Dto dto, CancellationToken cancellationToken = default)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entity = MapToEntity(dto);
        await _{{camel entity}}Repository.AddAsync(entity, cancellationToken);
        await _unitOfWork.SaveChangesAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return MapToDto(entity);
    }

    public async Task UpdateAsync(Guid id, {{entity}}Dto dto, CancellationToken cancellationToken = default)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entity = await _{{camel entity}}Repository.GetByIdAsync(id, cancellationToken)
            ?? throw new NotFoundException($"{{entity}} with id '{id}' was not found.");
        MapToEntity(dto, entity);
        await _{{camel entity}}Repository.UpdateAsync(entity, cancellationToken);
        await _unitOfWork.SaveChangesAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
    }

    public async Task DeleteAsync(Guid id, CancellationToken cancellationToken = default)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entity = await _{{camel entity}}Repository.GetByIdAsync(id, cancellationToken)
            ?? throw new NotFoundException($"{{entity}} with id '{id}' was not found.");
        await _{{camel entity}}Repository.RemoveAsync(entity, cancellationToken);
        await _unitOfWork.SaveChangesAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
    }

    private static {{entity}}Dto MapToDto({{entity}} entity) => new(
{{#each fields}}
        entity.{{name}}{{#unless @last}},{{/unless}}
{{/each}}
    );

    private static {{entity}} MapToEntity({{entity}}Dto dto, {{entity}}? target = null)
    {
        var entity = target ?? new {{entity}}();
{{#each fields}}
        entity.{{name}} = dto.{{name}};
{{/each}}
        return entity;
    }
}
