namespace {{ns}}.Application.Features.{{plural entity}}.Queries.Get{{entity}}List;

public class Get{{entity}}ListQueryHandler : IRequestHandler<Get{{entity}}ListQuery, List<{{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto>>
{
    private readonly {{ns}}.Application.Common.Interfaces.I{{entity}}Repository _repository;

    public Get{{entity}}ListQueryHandler({{ns}}.Application.Common.Interfaces.I{{entity}}Repository repository)
    {
        _repository = repository;
    }

    public async Task<List<{{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto>> Handle(Get{{entity}}ListQuery request, CancellationToken cancellationToken)
    {
        // SCAFFOLD:AI_IMPLEMENTATION:START:required
        var entities = await _repository.GetAllAsync(cancellationToken);
        // SCAFFOLD:AI_IMPLEMENTATION:END
        return entities.Select(x => new {{ns}}.Application.Features.{{plural entity}}.{{entity}}Dto({{#each fields}}{{#unless (eq (pascal name) "Id")}}x.{{pascal name}}{{#unless @last}}, {{/unless}}{{/unless}}{{/each}})).ToList();
    }
}
