import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import {{pascal pageName}} from './{{pascal pageName}}.jsx';

describe('{{pascal pageName}}', () => {
  it('renders without crashing', () => {
    render(<{{pascal pageName}} />);
    expect(screen.getByTestId('{{kebab pageName}}')).toBeInTheDocument();
  });
});
