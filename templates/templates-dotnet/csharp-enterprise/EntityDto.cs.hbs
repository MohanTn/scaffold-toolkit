namespace {{ns}}.Application.Features.{{plural entity}};

public record {{entity}}Dto(
    {{keyType}} Id{{#each fields}}{{#unless (eq (pascal name) "Id")}},
    {{type}} {{pascal name}}{{/unless}}{{/each}});
